<Settings>:
    BoxLayout:
        orientation:'vertical'
        id:'settings'
    
        Button:
            text:'About'
            on_press:root.about()
        Button:
            text:'Credential management'
            on_press:root.cred()
        Button:
            text:'back'
            on_press:root.back()
